---
brew_taps:
  - caskroom/versions	
  - jmespath/jmespath
  - AdoptOpenJDK/openjdk
  - shyiko/ktlint

brew_packages:
  # dev tools
  - ansible
  - git
  - git-standup
  - node
  - maven
  #
  - gnupg
  - pinentry-mac
  - ssh-copy-id
  #- tcptraceroute
  #- tig # ncurses-based text-mode interface for git
  - tmux # terminal multiplexer
  # Install GNU core utilities (those that come with OS X are outdated)
  # Don’t forget to add `$(brew --prefix coreutils)/libexec/gnubin` to `$PATH`.
  - coreutils
  # Install GNU `find`, `locate`, `updatedb`, and `xargs`, g-prefixed
  - findutils
#  - findutils postgres
#  - findutils maven
#  - findutils gradle
#  - findutils jenv
#  - findutils nvm
#  - findutils ansible
  # Install Bash 4
  # Note: don’t forget to add `/usr/local/bin/bash` to `/etc/shells` before
  # running `chsh`.
  - bash
  #
  - lynx
  - yle-dl
  # smarter cli
  # better cat
  - bat
  - prettyping
  # /usr/local/opt/fzf/install
  - fzf
  # ~/.gitconfig
  #[pager]
  #       diff = diff-so-fancy | less --tabs=1,5 -RFX
  #       show = diff-so-fancy | less --tabs=1,5 -RFX
  - diff-so-fancy
  - tldr
  - htop
  - p7zip
  # from Taps
  - openssh
  - screen
  - bash-completion2
  # monitor the progress of data through a pipe
  - pv
  # Command line interface to JMESPath
  - jp
  # Install some CTF tools; see https://github.com/ctfs/write-ups.
  #- zopfli
  - ktlint

brew_pkg_with_opts:
  - { pkg: "gnu-sed", opts: "with-default-names" }
  - { pkg: "grep", opts: "with-default-names" }
  - { pkg: "vim", opts: "override-system-vi" }
  - { pkg: "wget", opts: "enable-iri" }
  - { pkg: "imagemagick", opts: "with-webp" }

cask_appdir: /Applications
cask_packages:
  - dropbox
  #
  - firefox
  - google-chrome
  #
  - iterm2
  - the-unarchiver
  #
  - textmate
  - evernote
  - libreoffice
  # - yed
  #
  - telegram
  - slack
  - flowdock
  #
  - lisanet-gimp
  - xquartz
  - inkscape
  #
  - spotify
  - vlc
  #
  - intellij-idea
  - visual-studio-code
  - adoptopenjdk8
  # 
  - 1password
  - cyberduck
  - sourcetree
  - fork
  #
  - postman
  - soapui
  #
  - vagrant
  - virtualbox
  #
  - sequel-pro
  - pgadmin4
  # not found - sqldeveloper
  #
  - disk-inventory-x
  - spectacle
  - go2shell
  - namechanger
  # 
  - teamviewer
  - vnc-viewer
  # - clipy

#npm_modules:
